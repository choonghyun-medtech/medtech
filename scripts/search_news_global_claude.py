#!/usr/bin/env python3
"""
[사용 중지됨(2026-08-21) — 유료 API였음]

이 스크립트는 Anthropic API의 유료 web_search 도구($10/1,000회 검색 + 토큰)를 써서
해외 뉴스를 Google 검색 방식으로 수집하도록 만들었던 초안이다. 사용자가 "이 자동화는
최대한 돈을 쓰지 않는 방향으로 하고, 유료 서비스는 제시하지 말라"고 명확히 요청해
update-news.yml 워크플로에서 제외했다.

같은 목적(RSS가 못 잡는 기사를 md 원본 방식대로 site: 검색 + 기업명 검색으로 보강)은
**완전 무료**인 scrape_news_global_gsearch.py(Google 뉴스 검색 공개 RSS 엔드포인트,
API 키 불필요)로 대체했다. 실제 사용은 그쪽을 봐 주세요.

이 파일은 삭제 대신 무해한 상태로만 남겨둔다 — 혹시라도 실수로 워크플로에 다시
연결되거나 수동 실행되더라도 어떤 API도 호출하지 않고 즉시 0으로 종료해 과금이
발생하지 않도록 안전장치를 둔 것이다.
"""
import sys

if __name__ == "__main__":
    print("[INFO] 이 스크립트는 유료 API를 썼던 이전 버전이라 사용 중지됨(2026-08-21). "
          "무료 대체 스크립트 scrape_news_global_gsearch.py를 대신 실행하세요. "
          "이 스크립트는 어떤 네트워크 호출도 하지 않고 그대로 종료합니다.", file=sys.stderr)
    sys.exit(0)
